#include<stdio.h>
#include<string.h>
/*
    This code implements a simple Caesar cipher for encryption and decryption.
*/
/*
    This function encrypts the input string using a Caesar cipher.
*/
void encrypt(char *input, char *output, int shift){
     for(int i =0 ; input[i] != '\0'; i++){
        if (input[i]>='a' && input[i] <='z')
        {
            /* code */
            //need to add the Ceasar encryption logic
        }
        
     }
}

int main(){
    char str[100], encrypt[100],decrypt[100];
    int shift = 3;
    printf("Ceasar Cipher Encryption and Decryption\n");
    printf("Enter the string  to encrypt : \n");

};